# PIC Linux by Zabbix agent

> This is an official Linux template. It requires Zabbix agent 7.4 or newer.

## Vue d'ensemble

| Propriété | Valeur |
|---|---|
| Nom technique | `PIC Linux by Zabbix agent` |
| Nom d'affichage | PIC Linux by Zabbix agent |
| Version Zabbix | 7.4 |
| Fichier | `PIC Linux by Zabbix agent.yaml` |
| Groupes | PIC informatique - Template, Proxmox, Templates/Operating systems |
| Éléments (items) | 43 |
| Règles de découverte | 3 |
| Prototypes d'éléments | 25 |
| Déclencheurs | 17 |
| Macros | 28 |
| Cartographies de valeurs | 4 |
| Tableaux de bord | 3 |
| Tags | class: os, target: linux |

## Description

This is an official Linux template. It requires Zabbix agent 7.4 or newer.

Notes on filesystem (FS) discovery:
- The ext4/3/2 filesystem reserves space for privileged usage, typically set at 5% by default.
- BTRFS allocates a default of 10% of the volume for its own needs.
- To mitigate potential disasters, FS usage triggers are based on the maximum available space.
  - Utilization formula: 'pused = 100 - 100 * (available / total - free + available)'
- The FS utilization chart, derived from graph prototypes, reflects FS reserved space as the difference between used and available space from the total volume.

You can discuss this template or leave feedback on our forum https://www.zabbix.com/forum/zabbix-suggestions-and-feedback/387225-discussion-thread-for-official-zabbix-template-for-linux

Generated by official Zabbix template tool "Templator"

## Installation & configuration

## Overview

This is an official Linux template. It requires Zabbix agent 7.4 or newer.

#### Notes on filesystem (FS) discovery:
- The ext4/3/2 FS reserves space for privileged usage, typically set at 5% by default.
- BTRFS allocates a default of 10% of the volume for its own needs.
- To mitigate potential disasters, FS usage triggers are based on the maximum available space.
  - Utilization formula: `pused = 100 - 100 * (available / total - free + available)`
- The FS utilization chart, derived from graph prototypes, reflects FS reserved space as the difference between used and available space from the total volume.

## Macros

| Macro | Valeur par défaut | Description |
|---|---|---|
| `{$AGENT.TIMEOUT}` | 3m | Timeout after which agent is considered unavailable. Works only for agents reachable from Zabbix server/proxy (passive mode). |
| `{$CPU.UTIL.CRIT}` | 90 | Critical threshold of CPU utilization expressed in %. |
| `{$IF.ERRORS.WARN}` | 2 | Warning threshold of error packet rate. Can be used with interface name as context. |
| `{$IF.UTIL.MAX}` | 90 | Used as a threshold in the interface utilization trigger. |
| `{$IFCONTROL}` | 1 | Link status trigger will be fired only for interfaces where the context macro equals "1". |
| `{$KERNEL.MAXFILES.MIN}` | 256 |  |
| `{$KERNEL.MAXPROC.MIN}` | 1024 |  |
| `{$LOAD_AVG_PER_CPU.MAX.WARN}` | 1.5 | The CPU load per core is considered sustainable. If necessary, it can be tuned. |
| `{$MEMORY.AVAILABLE.MIN}` | 20M | Used as a threshold in the available memory trigger. |
| `{$MEMORY.UTIL.MAX}` | 90 | Used as a threshold in the memory utilization trigger. |
| `{$NET.IF.IFNAME.MATCHES}` | ^.*$ | Used for network interface discovery. Can be overridden on the host or linked template level. |
| `{$NET.IF.IFNAME.NOT_MATCHES}` | (^Software Loopback Interface\|^NULL[0-9.]*$\|^[Ll]o[0-9.]*$\|^[Ss]ystem$\|^Nu[0-9.]*$\|^veth[0-9A-z]+$\|docker[0-9]+\|br-[a-z0-9]{12}) | Filters out `loopbacks`, `nulls`, docker `veth` links and `docker0` bridge by default. |
| `{$SWAP.PFREE.MIN.WARN}` | 50 | The warning threshold of the minimum free swap. |
| `{$SYSTEM.FUZZYTIME.MAX}` | 60s | The upper threshold for difference of system time. |
| `{$SYSTEM.FUZZYTIME.MIN}` | 10s | The lower threshold for difference of system time. Used in recovery expression to avoid trigger flapping. |
| `{$VFS.DEV.DEVNAME.MATCHES}` | .+ | Used for block device discovery. Can be overridden on the host or linked template level. |
| `{$VFS.DEV.DEVNAME.NOT_MATCHES}` | ^(loop[0-9]*\|sd[a-z][0-9]+\|nbd[0-9]+\|sr[0-9]+\|fd[0-9]+\|dm-[0-9]+\|ram[0-9]+\|ploop[a-z0-9]+\|md[0-9]*\|hcp[0-9]*\|zram[0-9]*) | Used for block device discovery. Can be overridden on the host or linked template level. |
| `{$VFS.DEV.READ.AWAIT.WARN}` | 20 | The average response time (in ms) of disk read before the trigger fires. |
| `{$VFS.DEV.WRITE.AWAIT.WARN}` | 20 | The average response time (in ms) of disk write before the trigger fires. |
| `{$VFS.FS.FSNAME.MATCHES}` | .+ | Used for filesystem discovery. Can be overridden on the host or linked template level. |
| `{$VFS.FS.FSNAME.NOT_MATCHES}` | ^(/dev\|/sys\|/run\|/proc\|.+/shm$) | Used for filesystem discovery. Can be overridden on the host or linked template level. |
| `{$VFS.FS.FSTYPE.MATCHES}` | ^(btrfs\|ext2\|ext3\|ext4\|reiser\|xfs\|ffs\|ufs\|jfs\|jfs2\|vxfs\|hfs\|apfs\|refs\|ntfs\|fat32\|zfs)$ | Used for filesystem discovery. Can be overridden on the host or linked template level. |
| `{$VFS.FS.FSTYPE.NOT_MATCHES}` | ^\s$ | Used for filesystem discovery. Can be overridden on the host or linked template level. |
| `{$VFS.FS.INODE.PFREE.MIN.CRIT}` | 10 | The critical threshold of the filesystem metadata utilization. |
| `{$VFS.FS.INODE.PFREE.MIN.WARN}` | 20 | The warning threshold of the filesystem metadata utilization. |
| `{$VFS.FS.PUSED.MAX.CRIT}` | 95 | The critical threshold of the filesystem utilization. |
| `{$VFS.FS.PUSED.MAX.DISASTER}` | 98 | The disaster threshold of the filesystem utilization. |
| `{$VFS.FS.PUSED.MAX.WARN}` | 80 | The warning threshold of the filesystem utilization. |

## Éléments surveillés (items)

| Nom | Clé | Type | Type de valeur | Unité |
|---|---|---|---|---|
| Available memory | `vm.memory.size[available]` | ZABBIX_PASSIVE | UNSIGNED | B |
| Available memory in % | `vm.memory.size[pavailable]` | ZABBIX_PASSIVE | FLOAT | % |
| Checksum of /etc/passwd | `vfs.file.cksum[/etc/passwd,sha256]` | ZABBIX_PASSIVE | CHAR |  |
| Context switches per second | `system.cpu.switches` | ZABBIX_PASSIVE | FLOAT |  |
| CPU guest nice time | `system.cpu.util[,guest_nice]` | ZABBIX_PASSIVE | FLOAT | % |
| CPU guest time | `system.cpu.util[,guest]` | ZABBIX_PASSIVE | FLOAT | % |
| CPU idle time | `system.cpu.util[,idle]` | ZABBIX_PASSIVE | FLOAT | % |
| CPU interrupt time | `system.cpu.util[,interrupt]` | ZABBIX_PASSIVE | FLOAT | % |
| CPU iowait time | `system.cpu.util[,iowait]` | ZABBIX_PASSIVE | FLOAT | % |
| CPU nice time | `system.cpu.util[,nice]` | ZABBIX_PASSIVE | FLOAT | % |
| CPU softirq time | `system.cpu.util[,softirq]` | ZABBIX_PASSIVE | FLOAT | % |
| CPU steal time | `system.cpu.util[,steal]` | ZABBIX_PASSIVE | FLOAT | % |
| CPU system time | `system.cpu.util[,system]` | ZABBIX_PASSIVE | FLOAT | % |
| CPU user time | `system.cpu.util[,user]` | ZABBIX_PASSIVE | FLOAT | % |
| CPU utilization | `system.cpu.util` | DEPENDENT | FLOAT | % |
| Free swap space | `system.swap.size[,free]` | ZABBIX_PASSIVE | UNSIGNED | B |
| Free swap space in % | `system.swap.size[,pfree]` | ZABBIX_PASSIVE | FLOAT | % |
| Get filesystems | `vfs.fs.get` | ZABBIX_PASSIVE | TEXT |  |
| Host name of Zabbix agent running | `agent.hostname` | ZABBIX_PASSIVE | CHAR |  |
| Interrupts per second | `system.cpu.intr` | ZABBIX_PASSIVE | FLOAT |  |
| Load average (15m avg) | `system.cpu.load[all,avg15]` | ZABBIX_PASSIVE | FLOAT |  |
| Load average (1m avg) | `system.cpu.load[all,avg1]` | ZABBIX_PASSIVE | FLOAT |  |
| Load average (5m avg) | `system.cpu.load[all,avg5]` | ZABBIX_PASSIVE | FLOAT |  |
| Maximum number of open file descriptors | `kernel.maxfiles` | ZABBIX_PASSIVE | UNSIGNED |  |
| Maximum number of processes | `kernel.maxproc` | ZABBIX_PASSIVE | UNSIGNED |  |
| Memory utilization | `vm.memory.utilization` | DEPENDENT | FLOAT | % |
| Number of CPUs | `system.cpu.num` | ZABBIX_PASSIVE | UNSIGNED |  |
| Number of installed packages | `system.sw.packages.get` | ZABBIX_PASSIVE | UNSIGNED |  |
| Number of logged in users | `system.users.num` | ZABBIX_PASSIVE | UNSIGNED |  |
| Number of processes | `proc.num` | ZABBIX_PASSIVE | UNSIGNED |  |
| Number of running processes | `proc.num[,,run]` | ZABBIX_PASSIVE | UNSIGNED |  |
| Operating system | `system.sw.os` | ZABBIX_PASSIVE | CHAR |  |
| Operating system architecture | `system.sw.arch` | ZABBIX_PASSIVE | CHAR |  |
| System boot time | `system.boottime` | ZABBIX_PASSIVE | UNSIGNED | unixtime |
| System description | `system.uname` | ZABBIX_PASSIVE | CHAR |  |
| System local time | `system.localtime` | ZABBIX_PASSIVE | UNSIGNED | unixtime |
| System name | `system.hostname` | ZABBIX_PASSIVE | CHAR |  |
| System uptime | `system.uptime` | ZABBIX_PASSIVE | UNSIGNED | uptime |
| Total memory | `vm.memory.size[total]` | ZABBIX_PASSIVE | UNSIGNED | B |
| Total swap space | `system.swap.size[,total]` | ZABBIX_PASSIVE | UNSIGNED | B |
| Version of Zabbix agent running | `agent.version` | ZABBIX_PASSIVE | CHAR |  |
| Zabbix agent availability | `zabbix[host,agent,available]` | INTERNAL | UNSIGNED |  |
| Zabbix agent ping | `agent.ping` | ZABBIX_PASSIVE | UNSIGNED |  |

## Règles de découverte (LLD)

| Nom | Clé | Type | Prototypes d'éléments | Prototypes de déclencheurs |
|---|---|---|---|---|
| Block devices discovery | `vfs.dev.discovery` | ZABBIX_PASSIVE | 9 | 2 |
| Mounted filesystem discovery | `vfs.fs.dependent.discovery` | DEPENDENT | 7 | 1 |
| Network interface discovery | `net.if.discovery` | ZABBIX_PASSIVE | 9 | 3 |

## Déclencheurs (triggers)

| Nom | Sévérité | Description |
|---|---|---|
| Linux: FS [{#FSNAME}]: Filesystem has disappeared | Désastre | Le systeme de fichiers {#FSNAME} ne remonte plus de donnees ALORS QUE l'agent repond toujours (agent.ping vivant) : demontage ou disque sous-jacent disparu. La garde `and nodata(agent.ping,10m)=0` empeche le faux positif quand c'est l'hote ou le proxy de collecte qui est muet (toutes les metriques se taisent en meme temps) : dans ce cas l'alerte pertinente est `agent is not available` (hote/agent HS, proxy up) ou le trigger proxy-down cote serveur (proxy HS). |
| Linux: {#DEVNAME}: Disk has disappeared | Désastre | The block device {#DEVNAME} has stopped reporting data. It may have been disconnected or removed. |
| Linux: High memory utilization | Moyen | The system is running out of free memory. |
| Linux: Zabbix agent is not available | Moyen | For passive agents only, host availability is used with `{$AGENT.TIMEOUT}` as a time threshold. |
| Linux: High CPU utilization | Avertissement | CPU utilization is too high. The system might be slow to respond. |
| Linux: Interface {#IFNAME}: High bandwidth usage | Avertissement | The utilization of the network interface is close to its estimated maximum bandwidth. |
| Linux: Interface {#IFNAME}: High error rate | Avertissement | It recovers when it is below 80% of the `{$IF.ERRORS.WARN:"{#IFNAME}"}` threshold. |
| Linux: Number of installed packages has been changed | Avertissement |  |
| Linux: System time is out of sync | Avertissement | The host's system time is different from Zabbix server time. |
| Linux: {#DEVNAME}: Disk read/write request responses are too high | Avertissement | This trigger might indicate the disk `{#DEVNAME}` saturation. |
| Linux: {HOST.NAME} has been restarted | Avertissement | The host uptime is less than 10 minutes. |
| Linux: /etc/passwd has been changed | Information |  |
| Linux: Configured max number of open filedescriptors is too low | Information |  |
| Linux: Configured max number of processes is too low | Information |  |
| Linux: Interface {#IFNAME}: Ethernet has changed to lower speed than it was before | Information | This Ethernet connection has transitioned down from its known maximum speed. This might be a sign of autonegotiation issues. Acknowledge to close the problem manually. |
| Linux: Operating system description has changed | Information | The description of the operating system has changed. Possible reasons are that the system has been updated or replaced. Acknowledge to close the problem manually. |
| Linux: System name has changed | Information | The name of the system has changed. Acknowledge to close the problem manually. |

## Cartographies de valeurs (value maps)

| Nom | Correspondances |
|---|---|
| ifOperStatus | 0→unknown, 1→notpresent, 2→down, 3→lowerlayerdown, 4→testing, 5→dormant, 6→up |
| Linux::Interface protocol types | 0→from KA9Q: NET/ROM pseudo, 1→Ethernet, 2→Experimental Ethernet, 3→AX.25 Level 2, 4→PROnet token ring, 5→Chaosnet, 6→IEEE 802.2 Ethernet/TR/TB, 7→ARCnet, … (+56) |
| zabbix.host.available | 0→not available, 1→available, 2→unknown |
| Zabbix agent ping status | 1→Up |

## Tableaux de bord (dashboards)

- Filesystems
- Network interfaces
- System performance

---
*Documentation générée automatiquement à partir de l'export Zabbix.*
